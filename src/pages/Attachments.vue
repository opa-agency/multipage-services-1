<template><div class="prose prose-zinc dark:prose-invert max-w-4xl"><h1>Attachments</h1><p class="lead">Attachments are how you share things in Protocol — they allow you to send all sorts of files to your contacts and groups. On this page, we'll dive into the different attachment endpoints you can use to manage attachments programmatically.</p><h2>The attachment model</h2><p>The attachment model contains all the information about the files you send to your contacts and groups, including the name, type, and size.</p><h3>Properties</h3><div class="space-y-3"><div class="border border-zinc-900/10 dark:border-white/10 rounded-lg p-3"><div class="font-semibold text-sm"><code>id</code> <span class="text-zinc-500">string</span></div><p class="text-sm text-zinc-600 dark:text-zinc-400 m-0">Unique identifier for the attachment.</p></div><div class="border border-zinc-900/10 dark:border-white/10 rounded-lg p-3"><div class="font-semibold text-sm"><code>filename</code> <span class="text-zinc-500">string</span></div><p class="text-sm text-zinc-600 dark:text-zinc-400 m-0">The filename for the attachment.</p></div><div class="border border-zinc-900/10 dark:border-white/10 rounded-lg p-3"><div class="font-semibold text-sm"><code>file_size</code> <span class="text-zinc-500">integer</span></div><p class="text-sm text-zinc-600 dark:text-zinc-400 m-0">The file size of the attachment in bytes.</p></div><div class="border border-zinc-900/10 dark:border-white/10 rounded-lg p-3"><div class="font-semibold text-sm"><code>created_at</code> <span class="text-zinc-500">timestamp</span></div><p class="text-sm text-zinc-600 dark:text-zinc-400 m-0">Timestamp of when the attachment was created.</p></div></div><h2>List all attachments</h2><p>This endpoint allows you to retrieve a paginated list of all your attachments. By default, a maximum of ten attachments are shown per page.</p><div class="bg-zinc-900 dark:bg-zinc-950 rounded-lg p-4 my-6 overflow-x-auto"><div class="text-xs text-zinc-400 mb-2">GET /v1/attachments</div><pre class="text-zinc-200 m-0"><code>curl -G https://api.protocol.chat/v1/attachments \
  -H "Authorization: Bearer {token}" \
  -d limit=10</code></pre></div></div></template>
